#include<iostream>
#include<set>
using namespace std;
int main(){
	int N;
	cin>>N;
	set<int>s;
	while(N--){
		int m;
		cin>>m;
		s.insert(m);
		
	}
	cout<<s.size()<<endl;
	for(int v:s){
		cout<<v<<" ";
	}
	
}
